node_modules/
dist/
coverage/
coverage-upload/
junit/
*.tsbuildinfo
*.local
.DS_Store

# Shared coverage tooling, fetched on demand by `make coverage`.
.coverage-report.py
.env
.env.*
!.env.example

# Google service-account keys — never commit (pass via GOOGLE_APPLICATION_CREDENTIALS instead)
*service-account*.json
*-key.json
gcloud-key*.json
