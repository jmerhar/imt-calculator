# Public repo built by public GitHub Actions: always resolve from the canonical npm registry, so
# installs behind a corporate proxy don't write internal registry URLs into package-lock.json.
registry=https://registry.npmjs.org/
