// Extends Vitest's `expect` with jest-dom matchers (toBeInTheDocument, toHaveValue, …)
// for the component tests. Loaded via vite.config.ts `test.setupFiles`.
import "@testing-library/jest-dom/vitest";
