# Codecov is informational only — the real gate runs in CI, with its threshold in coverage.toml.
# Uploads use the CODECOV_TOKEN repo secret (tokenless upload is unreliable for this repo).
coverage:
  status:
    project:
      default:
        informational: true
    patch:
      default:
        informational: true

comment: false
