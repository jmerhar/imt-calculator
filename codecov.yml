# Codecov is informational only — the real gate is scripts/coverage-report.py --gate in CI.
# Public repo, so uploads are tokenless.
coverage:
  status:
    project:
      default:
        informational: true
    patch:
      default:
        informational: true

comment: false
